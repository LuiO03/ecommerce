<a
    href="{{ $src }}"
    data-title="{{ $title }}"
    data-description="{{ $description }}"
    class="pswp-gallery pswp-link"
>
    <img
        src="{{ $src }}"
        alt="{{ $alt }}"
        {{ $attributes }}
    >
</a>
